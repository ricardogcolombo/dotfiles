-- Prettier plugin configuration - DISABLED to avoid conflict with conform.nvim
-- return {
--   "prettier/vim-prettier",
--   build = "yarn install",
--   ft = { "javascript", "typescript", "css", "less", "scss", "json", "graphql", "markdown", "vue", "yaml", "html" },
--   config = function()
--     -- Prettier keybinding is handled in keymaps.lua
--   end,
-- }

-- Using conform.nvim for formatting instead
return {}

